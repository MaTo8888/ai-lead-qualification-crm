# Optional — leave both empty to run entirely in deterministic fallback mode.
# If ANTHROPIC_API_KEY is set, it is tried first; OPENAI_API_KEY is the
# second choice. Never commit a filled-in .env.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
